import * as React from 'react';
import { {{ camelCase name }} } from './';

const {{ pascalCase name }}Example: React.FC = () => {
  const result = {{ camelCase name }}();
  return <p />;
};

export { {{ pascalCase name }}Example };
