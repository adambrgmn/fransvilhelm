---
name: useDimensions
menu: Hooks
route: /hook/use-dimensions
---

import { Playground } from 'docz';
import { UseDimensionsExample } from './use-dimensions.tsx';

# useDimensions

Use this hook to debounce a value only updating it once within the given delay.
Useful if you need to request data when an input updates but don't want to fetch
the new data on every keystroke.

## Usage

```jsx
import React from 'react';
import { useDimensions } from '@adambrgmn/hooks';

const SquareWithDimensions = () => {
  const { ref, rect } = useDimensions();
  return (
    <div ref={ref}>
      {rect.width}px / {rect.height} px
    </div>
  );
};
```

# Example

<Playground>
  <UseDimensionsExample />
</Playground>

## Parameters

`useDimensions` accepts no parameters.

## Returns

`useDimensions` returns an object with two props – a ref object and the rect.

| Prop | Type       | Description                                                                                                                                       |
| :--- | :--------- | :------------------------------------------------------------------------------------------------------------------------------------------------ |
| ref  | RefObject  | Ref to be applied onto an element of choice                                                                                                       |
| rect | ClientRect | rect object containing dimensions and position of the current element. This is the same object as one gets from `element.getBoundingClientRect()` |
